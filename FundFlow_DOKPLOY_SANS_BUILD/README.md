# FundFlow — Dokploy sans build

Cette édition utilise l'image officielle `python:3.11-alpine` et monte `app.py` comme une configuration Docker Compose. Il n'y a ni Nixpacks, ni `Dockerfile`, ni installation de dépendances.

Suivre impérativement `DEPLOIEMENT_OBLIGATOIRE.txt`.
